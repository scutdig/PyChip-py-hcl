class Add(object):
    def __init__(self, left, right):
        self.left = left
        self.right = right


def do_add(left, right):
    # TODO: need some check
    print('do_add: need some check')

    return Add(left, right)
